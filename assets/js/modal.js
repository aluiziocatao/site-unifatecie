document.addEventListener('DOMContentLoaded', function () {
    const modalElement = document.getElementById('modalInicial');
    const modal = new bootstrap.Modal(modalElement);
    modal.show();
});